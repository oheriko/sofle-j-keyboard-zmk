# Use flake.nix for Nix development environment (optional)
# Only enable if you have direnv and nix installed
# Uncomment the line below to use:
use flake

# Also pull from any parent .envrc
source_up

# Load .env.local for environment variables
dotenv_if_exists .env
dotenv_if_exists .env.local
